# دليل تجهيز نسخة جديدة (نظيفة) من لوحة إدارة عمليات الأداء

هذا الدليل يشرح كيف يحصل شخص آخر على **نفس هذه اللوحة** لكن **بدون أي بيانات**،
وينشرها على حسابه الخاص. يمكن تسليمه كما هو لأي شخص لديه **Claude** (Claude Code)
ليقوم بالإعداد نيابةً عنه.

---

## ١) ما هي اللوحة؟

لوحة قياس أداء عربية (اتجاه من اليمين لليسار) لإدارة عمليات الأداء:
- **مؤشرات (KPIs)** × **قطاعات**، لكل خلية **مستهدف** و**منجز**.
- شاشة **النظرة العامة**: عدّادات نصف دائرية وحالات ملوّنة (متعثر / على المسار...).
- شاشة **تحديث المؤشرات**: يُدخل المستخدم القيمة المنجَزة مباشرةً، مع **تاريخ آخر تحديث**.
- **إدخال البيانات** لكل قطاع، وإدارة **المؤشرات والمستهدفات وعتبات الحالة**.
- **صلاحيات**: مدير الإدارة (كل شيء) · مدير قطاع (قطاعاته فقط).
- **تسجيل الدخول برقم الجوال** عبر رمز تحقق (OTP)، مع مبدّل **لغة (عربي/إنجليزي)** و**ثيمات**.

## ٢) نقطة مهمة: لماذا تبدأ النسخة الجديدة «فارغة» تلقائيًا؟

البيانات (القياسات المُدخلة) **لا تُخزَّن في الكود**، بل في **قاعدة البيانات**.
لذلك أي نسخة جديدة تُربط بـ **قاعدة بيانات جديدة خاصة بها** تبدأ **بدون أي قياسات**.
عند أول تشغيل يُنشئ النظام تلقائيًا فقط:
- **المؤشرات والقطاعات والمستهدفات الافتراضية** (يمكن تعديلها من داخل اللوحة).
- **حساب مدير الإدارة** (حسب رقم الجوال في الإعدادات).

> إذا رغب في مؤشرات/مستهدفات مختلفة تمامًا: يعدّلها بسهولة من داخل اللوحة
> (تبويبات: المؤشرات · المستهدفات · الهيكل التنظيمي)، أو من ملف `src/lib/db.ts`.

## ٣) المتطلبات التقنية

- **Node.js 20 أو أحدث** (المشروع يُبنى على Node 22).
- إطار العمل: **Next.js 15 (App Router) + React 19 + TypeScript**.
- الاعتماديات: `next`, `react`, `react-dom`, `pg`, `nodemailer`, `recharts`.
- **قاعدة بيانات Postgres** للحفظ الدائم (مثل **Neon** المجاني). بدونها يعمل على
  ملف محلي مؤقت يُمسح عند إعادة النشر — لذا القاعدة ضرورية للاستخدام الفعلي.
- حساب استضافة (مثل **Render** — نشر Docker مجاني عبر ملف `render.yaml` الموجود).

## ٤) كيفية الحصول على الكود (اختر واحدة)

1. **إضافة الشخص كمتعاون (Collaborator) على GitHub** ثم يستنسخ (clone) المستودع. الأنظف.
2. **جعل المستودع عامًا (Public) مؤقتًا** ليستنسخه أو يعمل له Fork.
3. **تنزيل الكود كملف ZIP** من GitHub (زر Code → Download ZIP) وإرساله، ثم يفتحه Claude لديه.

> بعد حصوله على الكود، لا يحتاج نسخ أي بيانات — القاعدة الجديدة تبدأ فارغة.

## ٥) متغيّرات البيئة المطلوبة

| المتغيّر | الغرض | القيمة |
|---------|-------|--------|
| `ADMIN_PHONE` | رقم جوال مدير الإدارة الأول (يُنشأ تلقائيًا) | `05XXXXXXXX` |
| `ADMIN_NAME` | اسم مدير الإدارة (اختياري) | اسم المدير |
| `DATABASE_URL` | رابط اتصال Postgres للحفظ الدائم | `postgresql://...neon.tech/...?sslmode=require` |
| `COOKIE_SECURE` | اجعله `true` مع HTTPS، و`false` مع HTTP الداخلي | `true` |
| `SEED_DEMO` | بيانات تجريبية؟ اتركه `false` للبدء نظيفًا | `false` |

**اختياري — لإرسال رمز الدخول عبر SMS** (وإلا يظهر الرمز على الشاشة):
`SMS_PROVIDER=generic` مع `SMS_API_URL` و`SMS_API_KEY` و`SMS_SENDER` —
أو `SMS_PROVIDER=twilio` مع `TWILIO_SID` و`TWILIO_TOKEN` و`TWILIO_FROM`.

## ٦) خطوات النشر (الأسهل: Render + Neon)

1. **قاعدة بيانات Neon:** أنشئ مشروعًا على [neon.com](https://neon.com) واختر Region قريبًا
   (مثل Frankfurt)، ثم انسخ **Connection string** كاملًا (بعد إظهار كلمة السر).
2. **الاستضافة على Render:** من [dashboard.render.com](https://dashboard.render.com):
   New + → **Blueprint** → اختر المستودع → Apply (يقرأ `render.yaml` تلقائيًا).
3. **أضف متغيّرات البيئة** في خدمة Render (خاصة `DATABASE_URL`، و`ADMIN_PHONE` برقم المدير الجديد).
4. احفظ — يبدأ النشر تلقائيًا. أول فتحة على الخطة المجانية قد تتأخر ~50 ثانية (طبيعي).

**للتشغيل محليًا للتجربة:**
```bash
npm install
cp .env.example .env.local   # ثم عدّل القيم
npm run dev                  # على http://localhost:3000
# أو للإنتاج:
npm run build && npm start
```

## ٧) تسجيل الدخول

- يدخل المستخدم **برقم جواله المسجّل**، ويصله **رمز مكوّن من 6 أرقام**.
- **بدون مزوّد SMS**: الرمز **يظهر على الشاشة** (مناسب للتجربة/الاستخدام الداخلي).
- **مع مزوّد SMS**: الرمز يصل رسالة نصية على الجوال الحقيقي.
- أول دخول يكون برقم `ADMIN_PHONE`، ثم يُضاف بقية المستخدمين من تبويب «المدراء والصلاحيات».

---

## ٨) نص جاهز للصقه لدى Claude عند المستلم

> انسخ الفقرة التالية وألصقها لـ Claude (Claude Code) بعد أن يفتح الكود:

```
هذا مشروع Next.js 15 (App Router) + React 19 + TypeScript: لوحة قياس أداء عربية (RTL)
تُخزّن بياناتها في Postgres عبر متغيّر DATABASE_URL (وإلا ملف محلي مؤقت).

أريد نشر نسخة جديدة نظيفة (بدون أي بيانات) على حسابي:
1) جهّز قاعدة Postgres جديدة (Neon مجاني) وأعطني خطوات نسخ رابط الاتصال.
2) انشر المشروع على Render باستخدام render.yaml الموجود (Docker).
3) اضبط متغيّرات البيئة: DATABASE_URL برابط قاعدتي، ADMIN_PHONE برقم جوالي،
   ADMIN_NAME باسمي، COOKIE_SECURE=true، SEED_DEMO=false.
4) وضّح لي أن النسخة الجديدة تبدأ فارغة تلقائيًا (لا قياسات)، وأنها تُنشئ فقط
   المؤشرات والقطاعات والمستهدفات الافتراضية وحساب المدير.
5) اشرح لي كيف أعدّل المؤشرات والقطاعات والمستهدفات من داخل اللوحة، وكيف أضيف مستخدمين.
6) (اختياري) اشرح كيف أربط مزوّد SMS لاحقًا عبر متغيّرات SMS_*.
ابدأ بالخطوة الأولى، وطبّق خطوة خطوة، واطلب مني ما تحتاجه.
```

---

## ٩) ملاحظات مهمة

- **لا تشارك رابط `DATABASE_URL` أو أي مفتاح** علنًا — يحتوي كلمة سر.
- إذا أراد بدايةً **بلا مؤشرات افتراضية** (يعرّف كل شيء بنفسه): يمسح/يعدّل
  `DEFAULT_INDICATORS` و`DEFAULT_SECTORS` و`DEFAULT_TARGETS_BY_SECTOR` في `src/lib/db.ts`،
  أو ببساطة يعدّلها من داخل اللوحة بعد النشر.
- كل نسخة **منفصلة تمامًا**: قاعدة بياناتها ومستخدموها وبياناتها خاصة بها فقط.
