# مشروع لوحة متابعة الأداء — أداء (Adaa Dashboard)

## نظرة عامة
لوحة متابعة HTML أحادية الملف (`project-dashboard.html`) لمشروع "دعم وتفعيل قياس وإدارة أداء الاستراتيجيات الوطنية والأداء المؤسسي" التابع لمركز أداء.
اللغة: عربي RTL بالكامل. الخط: IBM Plex Sans Arabic.

## معلومات المشروع الأساسية
- المدة: **18 شهراً** (أبريل 2026 → سبتمبر 2027) — متغير `projectMonths` قابل للتعديل
- الأشهر 16–18 كمياتها وفواتيرها أصفار (لم تُوزَّع بعد)
- إجمالي العقد شامل الضريبة: 11,870,208 ر.س
- الفريق: عمر العتيق (مدير المشروع)، لمى المبدل، سلطانه العرجاني
- كل التفاصيل الكاملة في `project-info.md`

## المخرجات الأربعة
| المخرج | المستهدف | الوحدة | سعر الوحدة |
|--------|---------|--------|-----------|
| م1 مراجعة الاستراتيجيات | 60 | تقرير | 81,504 |
| م2 قياس المؤشرات | 290 | خدمة | 2,400 |
| م3 التزام التوثيق | 1,230 | خدمة | 768 |
| م4 الطلبات الواردة | 22 | تقرير | 172,320 |

## البنية التقنية
- ملف HTML واحد يحتوي CSS + JS
- **Supabase** للحفظ السحابي:
  - URL: `https://dgmakuenllpuzuupmeuf.supabase.co`
  - يستخدم supabase-js v2 من CDN مع anon key (موجود في الملف)
  - الجداول: `monthly_actuals`, `detail_cols`, `detail_rows`, `app_users`, `code_sessions`
  - **RLS مفعّل** — كل الإعداد في ملف واحد: `supabase/setup.sql` (يُشغَّل من SQL Editor، idempotent)
    - الدخول باسم مستخدم + رمز — يتطلب تفعيل Anonymous Sign-Ins في Supabase
    - القراءة: يحتاج صلاحية `view`
    - المنجز الشهري: يحتاج `edit:monthly`
    - تفاصيل مخرج: يحتاج `edit:details:N` — السياسة تقارن `output_idx + 1` بالصلاحية
    - `admin` = صلاحية كاملة: كل شيء + نافذة الإعدادات (ليست «مدير المشروع»)
    - الزائر بلا دخول لا يملك أي صلاحية ⇒ لا يقرأ ولا يكتب
  - `app_users` بلا أي سياسة ⇒ لا يُقرأ من الواجهة؛ كل وصول عبر دوال `security definer`
  - **مهم:** REST API المباشر يرجع 403 "Host not in allowlist" — يجب استخدام supabase-js client فقط (دالة `sbFetch` تترجم مسارات REST لاستدعاءات client)
- **النشر:**
  - **المستودع:** `Adaa-dashboard/dashboard` — نُقل من حساب شخصي إلى منظمة
    `Adaa-dashboard` حتى يخلو رابط المشاركة من اسم المستخدم.
    تطبيق Claude مثبَّت على المنظمة بصلاحية قراءة وكتابة
  - **الرابط المعتمد:** `https://adaa-project.netlify.app` — Netlify مربوط بالمستودع،
    ينشر تلقائياً مع كل دمج في `main`. **بقي يعمل بعد نقل المستودع ولم يحتج
    إعادة ربط** — لا تُعِد إنشاء الموقع على Netlify
  - ملفات اللوحة في مجلد **`site/`** وحده — **لا تضع فيه `package.json` أبداً**:
    المستودع يحتوي مشروع Next.js لا علاقة له باللوحة، وكان Netlify يكتشفه ويحاول
    بناءه فيفشل النشر بـ "Failed Due To Plugin Error". جعل `site/` قاعدة البناء
    (`base` في `netlify.toml`) يمنع اكتشاف أي إطار عمل من الأساس — وهو الحل الذي
    نجح؛ `NETLIFY_NEXT_PLUGIN_SKIP` لم يكفِ
  - `index.html` في الجذر تحويل إلى `./site/` · و`site/index.html` تحويل إلى اللوحة
  - **GitHub Pages** نسخة احتياطية على `https://adaa-dashboard.github.io/dashboard`
    — رابط المنظمة خالٍ من اسم المستخدم، فسببُ تفضيل Netlify الأصلي
    (أن Pages يفرض `<username>.github.io` للمستودعات الشخصية) لم يعد قائماً.
    ومع ذلك يبقى رابط Netlify هو المعتمد للمشاركة
  - لوحة استقطاب المواهب تبقى على `/hr-dashboard/`
- XLSX.js من CDN لاستيراد Excel

## الدخول والصلاحيات
- شاشة الدخول `#auth-gate` تغطي الصفحة **افتراضياً** (fail-closed) — حقلان: اسم المستخدم + الرمز
- تحمل **شعار أداء** (`img.auth-logo`) أعلى البطاقة البيضاء بدل أيقونة القفل، وتحته
  خط ذهبي/أخضر (`.auth-rule`) واسم المشروع كاملاً، وفي الأسفل اسم المركز (`.auth-foot`)
  - الشعار هو نفس base64 المستخدَم في الشريط العلوي — لا تضف نسخة ثانية منه للملف
  - `#auth-gate::before` توهّج قطري بلوني الهوية خلف البطاقة، و`overflow:auto` على الغلاف
    حتى لا تُقتطع البطاقة على الشاشات القصيرة
- `authSignIn()`: `signInAnonymously()` ثم `rpc('login', {p_username, p_code})`
  - الرمز مُجزّأ bcrypt في `app_users.code_hash`، والتحقق داخل الخادم — لا يُخزَّن في الواجهة
  - اسم المستخدم يُوحَّد بـ `norm_user()` (حروف صغيرة، بلا مسافات طرفية)
- `authInit()` عند `load`: جلسة موجودة ⇒ يقرأ `code_sessions` ⇒ `startApp(name, scopes)` ⇒ `loadFromCloud()`
- `myScopes` + `can(scope)` تحكمان الواجهة:
  - بلا أي صلاحية تعديل ⇒ `body.view-only`
  - بلا `edit:monthly` ⇒ `body.no-monthly`
  - `gateDet()` في نهاية `renderDetTable()` تقفل الجدول حسب `edit:details:N` (`body.det-locked`)
  - `admin` ⇒ `body.is-admin` يُظهر زر ⚙️
- **الواجهة تجميلية فقط** — الحماية الحقيقية في RLS؛ لا تعتمد على إخفاء الأزرار

## نافذة الإعدادات ⚙️ (لصاحب صلاحية `admin` فقط)
- `openSettings()` / `suLoad()` / `suEdit()` / `suSave()` / `suDelete()` / `suReset()`
- تستدعي دوال `security definer` تتحقق داخلياً من `has_scope('admin')`:
  - `admin_list_users()` — لا تُرجع أي رمز
  - `admin_save_user(p_username,p_name,p_code,p_scopes,p_active)` — `p_code` فارغ = أبقِ الرمز
  - `admin_delete_user(p_username)`
- حمايات: لا يمكن حذف آخر حساب نشط بصلاحية `admin` ولا سحبها منه
- تعديل الصلاحيات يحدّث `code_sessions` فوراً؛ إيقاف الحساب يُنهي جلساته
- أول حساب `admin` يُنشأ من SQL Editor: `select public.bootstrap_admin('user','code','الاسم');`
  (الدالة محجوبة عن `authenticated` و`anon`)

### الصلاحيات
`view` · `edit:monthly` · `edit:details:1..4` · `admin` — معرّفة في `SCOPE_LIST` بالكود وفي `setup.sql`

## سجل التعديلات 📜
- جدول `audit_log` — يُكتب من **triggers داخل قاعدة البيانات**، فالواجهة لا تستطيع تجاوزه أو تزويره
- `monthly_actuals`: trigger صفّي (`audit_row`) يسجّل القيم قبل/بعد ويتجاهل التحديثات التي لم تغيّر شيئاً
- `detail_cols`/`detail_rows`: triggers **على مستوى الجملة** بجداول انتقالية (`audit_detail_old/new`)
  — لأن الحفظ يحذف كل الصفوف ويعيد إدراجها، والتسجيل صفّاً صفّاً يغرق السجل
- يسجَّل أيضاً: `LOGIN` من دالة `login()`، و`USER_SAVE`/`USER_DELETE` من دوال الإدارة
- القراءة: `audit_recent(p_limit)` — تتحقق من `has_scope('admin')`؛ وسياسة RLS تحصرها بصاحب الصلاحية الكاملة
- التنظيف: `select public.audit_prune(180);` من SQL Editor (محجوبة عن العملاء)
- الواجهة: تبويب داخل نافذة ⚙️ — `suTab()` / `auLoad()` / `auWhat()` / `auChange()`

## خطة الكميات والفواتير
- تُحفظ سحابياً في جدول `plan_months` (كانت في المتصفح فقط) — صلاحية `edit:plan`
- `savePlanMonth(m)` فوري · `savePlanSoon(m)` مؤجَّل 1.2 ث للخلايا · `savePlanAll()` بعد رفع Excel
- `exportMPlan()` ينزّل قالب xlsx بالخطة الحالية، و`importMPlan(e)` يقرأه ويحدّث MP/INV ثم يحفظ
  - القالب: الشهر · اسم الشهر · موعد الاستحقاق · كمية م1..م4 · الفاتورة بدون الضريبة
  - المطابقة بعمود «الشهر» (1..projectMonths)، وتُتجاهَل الصفوف خارج المدى
- بلا `edit:plan` ⇒ `body.no-plan` يخفي زر الرفع ويعطّل خلايا صفحة الصرف

## شريط تقدم المشروع
- `renderProgress()` تملأ `#proj-progress` — **كانت معرّفة ولا تُستدعى**، فكان الشريط يظهر أبيض فارغاً؛
  صارت تُستدعى من `renderDash()`
- مربعان فقط: **التقدم الفعلي للمشروع** (`overallAct`) و**المستهدف حتى الشهر الحالي** (`overallPlan`)،
  والفجوة داخل شارة الحالة (أخضر/أصفر/أحمر) — شريط التقدم السفلي أُزيل بطلب المستخدم

## تنبيه اختلاف التقرير الشهري عن التفاصيل ⚠️
- **الصفحة الرئيسية لا تزال تقرأ من `acts` المُدخَل يدوياً** — هذه المقارنة تُنبّه فقط ولا تغيّر رقماً
- `doneDateCol(idx)` عمود تاريخ الإنجاز: أولوية «إنجاز» ← «تقييم» ← `dcol('recv')`
  (م1/م2 الاستلام من الشركة · م3 تاريخ التقييم · م4 تاريخ الإنجاز)
- `projMonthOf(v)` تحوّل التاريخ لرقم شهر المشروع (الشهر 1 = أبريل 2026) أو 0 خارج المدى
- `calcAct(idx,m)` تجمع `itemCount` لكل صف وقع تاريخ إنجازه في ذلك الشهر
- **`doneRange(idx)` = من أول شهر فيه تاريخ إنجاز حتى `curM`** — المقارنة خارجه بلا معنى،
  فقبل بدء تعبئة التفاصيل سيبدو كل شهر «مختلفاً» (بدونه ظهرت 14 خانة بدل 7 في بيانات الاختبار)
- `actDiffs()` تُرجع كل خانة مختلفة · `inDoneRange(i,m)` تحكم ظهور العلامة
- العرض: `.act-warn` داخل خلية «الفعلي» في `renderTable()` مع تلميح بالرقمين،
  و`#sync-warn` شريط ملخّص فوق الجدول
- الضغط على العلامة ⇒ `openDetMonth(i,m)` تنقل لصفحة التفاصيل مفلترة على ذلك الشهر

## فلتر «هذا الشهر» في التفاصيل 📅
- `detMonth[4]` (0 = كل الأشهر) — يفلتر حسب **تاريخ الإنجاز** لا الإرسال،
  فيطابق تماماً ما يحسبه `calcAct` ويشرح رقم الشهر
- `toggleDetMonth()` تستخدم `curM` · `renderDetMonthBar()` ترسم الزر في `#det-month-bar`
- مدمج في `detFilterRows` مع بقية الفلاتر، ويُصفَّر مع `clearColFilters()`
- البطاقات وعدّاد «عرض س من ص» يعكسان الفلتر تلقائياً

## تنبيهات التأخير
- `collectAlerts()` تمسح م1 و م2 (`TRACKED`) وتلتقط كل بند: له موعد تسليم مخطط،
  ولم يُسجَّل له تاريخ استلام، ومرّ موعده أو حلّ اليوم — مرتّبة بالأكثر تأخراً
- `renderAlerts()` تعرضها في `#alerts-box` أعلى الصفحة الرئيسية، وتُستدعى من `renderDash()`
- العدّاد يُحسب من تاريخ اليوم في كل فتح، فيزيد يوماً بيوم حتى يُسجَّل الاستلام
- بلا تنبيهات ⇒ صندوق أخضر «لا توجد مخرجات متأخرة»
- `arDays(n)` للصياغة العربية (يوم واحد / يومان / أيام / يوماً)
- `toggleAlerts()` تطوي الصندوق بسهم صغير في رأسه؛ الحالة تُحفظ في
  `localStorage['adaa_alerts_open']` (بـ try/catch) فتبقى بين الجلسات لكل متصفح
- عند الطي يبقى في الرأس: العدد + أكثر البنود تأخراً
- تنبيه: `overallAct()` تجمع كل الأشهر المُدخلة بينما `overallPlan()` تجمع حتى `curM` فقط —
  فإن كان `curM` متأخراً عن آخر شهر فيه بيانات ستبدو الفجوة أكبر مما هي

## توحيد صيغة التواريخ 📅
- **الصيغة المعتمدة في كل الجداول: `YYYY-MM-DD`** — لا تُغيّرها، فهي مشتركة بين
  الواجهة وملف الإكسل وتقرير PDF ومقارنات `pDate()`
- `isDateCol(idx,ci)` = عنوان العمود يحوي «تاريخ» (الإرسال · المخطط · الاستلام)
- `normDate(v)` تقبل: كائن `Date` · `YYYY-M-D` · `D/M/YYYY` · `D-M-YY` · `D.M.YYYY`
  · أرقام هندية (٠١٢) · الرقم التسلسلي لإكسل · ISO بوقت — وتُرجع `YYYY-MM-DD`
  - **اتجاه `D/M` يُستنتج من العمود كله** لا من الخلية: `colDayFirst(idx,ci)` تمسح كل قيم
    العمود — صفٌ واحد جزؤه الأول > 12 يحسم أنه يوم/شهر، والعكس بالعكس؛ وبلا دليل
    الافتراضي يوم/شهر. التخمين لكل خلية على حدة كان يقرأ `08/10/2026` (10 أغسطس
    بصيغة شهر/يوم) على أنها 8 أكتوبر
  - `normDate(v, dayFirst)` — الوسيط الثاني يأتي من `colDayFirst`، وافتراضه `true`
  - تاريخ غير صالح (31/02) أو نص («لا يوجد») **يبقى كما هو** — لا تُمسح المعلومة أبداً
- `normalizeDetDates(idx)` توحّد كل خلايا أعمدة التاريخ · `normalizeAllDates()` للمخرجات الأربعة
- نقاط الاستدعاء: `recalcTracked` (قبل الحساب) · `loadFromCloud` (مرة واحدة ثم تحفظ التصحيح
  لمن يملك `edit:details:N`) · `impRun` (الدمج والاستبدال) · `pasteTable` · `cellEdit` · INIT
- **`sheetAOA(ws)`** تقرأ ورقة إكسل خلية خلية: خلية التاريخ (`cell.t === 'd'`) تُؤخذ من
  كائن `Date` لا من `cell.w` — **هذا جوهر الإصلاح**: `raw:false` كان يُرجع النص المنسّق
  بصيغة الملف، فخلية بصيغة `mm/dd/yyyy` تُقرأ `05/01/2026` وتُفسَّر 5 يناير بدل 1 مايو.
  و`XLSX.read` صارت `{cellDates:true}`
- خلية التاريخ في الجدول **حقل نص** لا `input[type=date]`: الأخير يعرض التاريخ بلغة
  المتصفح (`mm/dd/yyyy` على جهاز إنجليزي) فيختلف الشكل بين جهاز وآخر وعن ملف الإكسل
  - `input.cell-date` بـ `dir=ltr` و`tabular-nums` و`placeholder="YYYY-MM-DD"`
  - قيمة ليست تاريخاً ⇒ صنف `.not-a-date` بخلفية برتقالية، فتُرى بلمحة ولا تُمسح
- ~~`swapDM(ci)` وزر `⇄`~~ **أُزيلا بطلب المستخدم** — بعد تصحيح التواريخ يدوياً صار
  الزر خطراً: ضغطة واحدة بالخطأ تقلب عموداً كاملاً. الوقاية الآن في `colDayFirst`
  و`sheetAOA`، وإن عاد الانقلاب فالحل إعادة رفع الملف الأصلي بـ «استبدال كامل»
- `cellEdit` تمرّر قيمة عمود التاريخ من `normDate` ثم تُعيد `recalcTracked` + `renderDetTable`
  فتتحدّث «الحالة» و«الأيام» فوراً (كانتا تنتظران تبديل التبويب)
- `gateDet()` صارت تعطّل `textarea` و`select` في الجسم أيضاً — كانت تعطّل `input` وحده،
  والفلاتر في الرأس تبقى شغّالة للجميع

## ترتيب الصفوف ↕️ (جداول التفاصيل)
- **الافتراضي في المخرجات الأربعة: أول عمود تاريخ، تنازلياً (الأحدث أولاً — الجديد فوق)**
  — م1 «تاريخ التسليم للشركة» · م2 «تاريخ الإرسال للشركة» · م3 «تاريخ التقييم» · م4 «تاريخ الاستلام»
- `detSort[4]`: `undefined` = لم يُحدَّد بعد فيأخذ الافتراضي · `null` = ألغاه المستخدم · `{ci,dir}`
- `applySort(idx)` ترتّب **`detRows` نفسها** لا العرض فقط، فيصل الترتيب لملف Excel
  وتقرير PDF والسحابة. ترتيب JS مستقر فالمتساوي يبقى بترتيبه
- `cmpCell` حسب نوع العمود: تاريخ (`pDate`) · رقم (العدد/الأيام) · نص (`localeCompare('ar')`)
  - **الفارغ وغير التاريخ («لايوجد») يذهبان لآخر الجدول في الاتجاهين** — لا يتصدّران أبداً
- `firstDateCol(idx)` أول عمود عنوانه يحوي «تاريخ» · `autoSort` تطبّق الافتراضي على غير المحدَّد
- زر `⇅` في `.th-tools` فوق العنوان: الاتجاه الطبيعي ← عكسه ← بلا ترتيب — `setSort(ci)`
  - `firstDir(idx,ci)` = عمود التاريخ يبدأ تنازلياً (`-1`)، وغيره تصاعدياً (`+1`)
  - **اللقطة (`detSnap`) قبل تغيير `detSort`** وإلا سجّلت الحالة الجديدة فيفسد التراجع
  - اللقطة تحفظ `sort` أيضاً، و`detUndoLast` تعيدها
- نقاط الاستدعاء: `loadFromCloud` (مع التوحيد، ثم حفظ ما تغيّر) · INIT · `impRun` (الوضعان،
  ويُصفَّر `detSort` ليأخذ الملف الجديد الافتراضي) · `pasteTable` · `cellEdit` (إن كان العمود المعدَّل هو عمود الترتيب)
- `delCol`/`delLastCol` تصحّحان `detSort[idx].ci` بعد إزاحة الأعمدة، و`applySort`
  تُلغي الترتيب إن خرج الفهرس عن المدى

## التراجع ↩️ (جداول التفاصيل)
- مكدّس لقطات **مستقل لكل مخرج**: `detUndo[4]`، حد أقصى `UNDO_MAX = 30` لقطة
- `detSnap(idx)` تلتقط نسخة عميقة من `detCols`/`detRows` + حالة الفلاتر
  (`detColFilter`/`detLateOnly`) **قبل** التغيير — تُستدعى من:
  `cellEdit` · `headEdit` · `addRow` · `delRow` · `addCol` · `delCol` · `delLastCol`
  · `detDrop` · `pasteTable` (المسارَان) · `impRun` (الدمج والاستبدال)
- في `impRun` اللقطة **بعد** التأكيد والتحقق من العناوين، حتى لا تُسجَّل لقطة لعملية أُلغيت
- `detUndoLast()` تسحب آخر لقطة، تعيدها، ترسم الجدول، ثم `saveDetailToCloud` —
  فالتراجع يصل السحابة أيضاً ولا يعود القديم عند إعادة التحميل
- تعديل الخلية/العنوان صار عبر `cellEdit(ri,ci,val)` و`headEdit(ci,val)` بدل الإسناد
  المباشر داخل `onchange` — الدالة تتجاهل التغيير إن كانت القيمة نفسها فلا لقطات فارغة
- `addCol`/`delCol`/`delLastCol` صارت تحفظ سحابياً (`saveDetSoon`) — **كانت لا تحفظ إطلاقاً**
  فتضيع تغييرات الأعمدة عند إعادة التحميل
- الزر `#dt-undo` في `.dt-btns` يعرض عدد اللقطات ويُعطَّل عند الفراغ (`undoBtnSync()`
  تُستدعى من نهاية `renderDetTable` ومن `detSnap`)
- Ctrl+Z يعمل فقط إذا كانت صفحة التفاصيل مفتوحة (`#details-view.on`) والهدف ليس
  حقل إدخال — داخل الحقول يبقى تراجع المتصفح النصي كما هو
- المكدّس في الذاكرة فقط: يُفرَّغ بإعادة تحميل الصفحة (السجل الدائم هو `audit_log`)

## شريط أدوات جدول التفاصيل
- الظاهر دائماً: **↩️ تراجع** · **📄 نسخ الجدول** · **⬇️ تصدير Excel** · **⬆️ رفع Excel**
- `copyDetTable()` تنسخ الجدول للحافظة مفصولاً بـ Tab وسطرٍ لكل صف، فيُلصق في
  Excel و Word كجدول حقيقي. تنسخ **المعروض بعد الفلترة** وتذكر ذلك في الرسالة،
  وتُسطّح أي سطر جديد داخل الخلية حتى لا ينكسر الصف. النسخ قراءة فقط ⇒ يظهر للجميع
  - `copyFallback(tsv)` نافذة فيها النص محدَّداً للنسخ اليدوي إن منع المتصفح الكتابة
- **⚙️ أدوات الجدول** قائمة منسدلة تجمع: إضافة صف · إضافة عمود · حذف آخر عمود · لصق جدول
  — `toggleDtMenu` / `closeDtMenu` / `dtMenu(name)`؛ تُغلق بالضغط خارجها وقبل تنفيذ أي أمر
  (وإلا بقيت مفتوحة فوق نافذة التأكيد)
- `body.det-locked` صار يخفي `.dt-edit` وحدها لا `.dt-btns` كلها — **التصدير يبقى لمن يقرأ فقط**
- **منطقة «اسحب وأفلت ملف Excel» أُزيلت** — كانت تستدعي `importXL` نفسها، فهي تكرار حرفي
  لزر «رفع Excel» بلا أي فرق
- شرح الحالات والصيغ تحت الجدول (`.det-legend`) **لصاحب `admin` وحده** عبر `body.is-admin`

## جداول التفاصيل — الحفظ والاستيراد
- **`saveDetailToCloud(idx)` يُدرج الجديد أولاً ثم يحذف القديم بمعرّفاته.**
  الترتيب مقصود: الطريقة السابقة (حذف كل الصفوف ثم إدراجها) كانت تفقد الجدول
  بالكامل إن فشل الإدراج — وهما طلبان منفصلان لا معاملة واحدة. الأخطاء تُعرض
  عبر `sbErr()` بدل الابتلاع الصامت
- `saveDetSoon(idx)` مؤقّت منفصل لكل مخرج — كان مشتركاً (`window._detSave`)
  فيضيع الحفظ أو يُكتب على المخرج الخطأ عند التنقل السريع بين التبويبات
- `exportDetXL()` ينزّل الجدول كاملاً xlsx — **وأعمدة التاريخ تُكتب خلايا تاريخ حقيقية**
  (`t:'d'`) بصيغة `yyyy-mm-dd` عبر `cell.z`، و`aoa_to_sheet`/`writeFile` بـ `{cellDates:true}`.
  كتابتها نصاً كانت تجعل إكسل يحوّل الخلية عند تعديلها لصيغة الجهاز المحلية، فيصير
  العمود مختلطاً وتنقلب التواريخ عند إعادة الرفع
- **`pasteTable()` يمرّ من نفس مسار الرفع**: يحوّل ما في الحافظة إلى AOA ثم يستدعي
  `openImp(idx, [{name:'المُلصَق', aoa}])` — فالأعمدة تُطابَق بالاسم ثم بالمعنى والدمج هو
  الافتراضي. كان يستبدل الجدول كله بعناوين ما لُصق
  - `openImp(idx, sheets)` مستخرجة ليشترك فيها الرفع واللصق؛ تخفي حقل اختيار الورقة
    (`.imp-sheet-field`) إن كانت ورقة واحدة
  - تعذّر قراءة الحافظة ⇒ نافذة فيها `textarea` تلصق فيه يدوياً ثم «متابعة»
- **`openImp(idx, sheets, mode)`** — الوسيط الثالث يحدّد الوضع المختار مسبقاً
- **وضع «إضافة صفوف جديدة» (`append`)** — **اختياري لا افتراضي**:
  كل سطر يصير صفاً جديداً، ولا يُمسّ أي صف موجود إطلاقاً. لا يحتاج عمود المفتاح
  (شرط وجود العمود الأول خاصٌّ بالدمج وحده)
  - **كان افتراضي اللصق فسبّب تكرار البيانات**: لصق جدول فيه صفوف موجودة كان
    يضيفها نسخةً ثانية بدل تحديثها. `pasteRun` ما عادت تمرّر `'append'`،
    فرجع الافتراضي **دمج** — كما في رفع Excel تماماً. لا تُعِد الافتراضي إلى
    `append`: الدمج يضيف الجديد أصلاً، فلا شيء يُكسب من التكرار
- **`dupKeys(idx)`** تجمع قيم العمود الأول المتكررة، و`renderDetTable` تعطي صفوفها
  صنف `.dup-row` (خلفية حمراء وحدّ أحمر ونص أحمر) مع شريط `#det-dup` فوق الجدول.
  الحساب مشتق من البيانات لا مخزَّن، فيبقى صحيحاً بعد إعادة التحميل وأي تعديل
- **`todayISO()` + خانة `#imp-today`** — تختم عمود تاريخ الإرسال (`sendDateCol(idx)`:
  عنوان يحوي «إرسال»/«ارسال»، وإلا أول عمود تاريخ) بتاريخ اليوم
  **للصفوف الجديدة الفارغة وحدها** — لا تُغيَّر تواريخ صف موجود ولا تاريخ جاء في المُلصَق
- **`keyOf` لم يعد يطابق «حالة» و«أيام» بالمعنى** — عمود مثل «أيام العمل» في إيميل الشركة
  كان يقع في «الأيام (تأخير/متبقي)» المحسوب. المطابقة الحرفية بالاسم تكفي للدورة الكاملة
- `importXL(e)` يفتح **نافذة رفع** (`#imp-modal`) فيها:
  - **اختيار الورقة** — ملف إكسل قد يحوي أوراقاً عدة، والافتراضي آخر ورقة
    (كان يقرأ الورقة الأولى فقط فيستورد النسخة القديمة)
  - **الطريقة**: `دمج` (افتراضي) أو `استبدال كامل` مع تأكيد
- الدمج: يطابق الأعمدة بالاسم، ثم **بالمعنى** (`مخطط`/`استلام`/`حالة`/`أيام`/`العدد`)
  لو اختلفت التسمية. المفتاح هو العمود الأول؛ الموجود يُحدَّث والجديد يُضاف بلا
  تكرار، والخلية الفارغة لا تمسح قيمة، والعناوين لا تتغير
- الاستبدال: يعتمد عناوين الملف وصفوفه، ثم `ensureTrackedCols` + `migrateStatusLast`

## عمود «العدد»
- صف واحد قد يمثّل عدة بنود (ملف فيه 79 مؤشراً). عمود اسمه «العدد» يحمل الرقم
- بطاقات الملخص **تجمع العدد** بدل عدّ الصفوف، وبطاقة الإجمالي تعرض «173 · 57 سطر»
- إن لم يوجد العمود (م1 مثلاً) فكل صف = 1
- **الفلترة على العناوين:** قائمة اختيار داخل رأس كل عمود، خياراتها القيم
  الموجودة فعلاً في ذلك العمود (`colOptions`). الفلاتر تتراكم بـ AND،
  والمفعَّل منها يُلوَّن ذهبياً، ويظهر شريط «عرض س من ص» مع زر مسح
  - الحالة في `detColFilter[outputIdx]` — مستقلة لكل مخرج
  - `detFilterRows()` تُرجع `[row, ri]` محافظةً على الفهرس الأصلي،
    فالتعديل على صف مفلتر يصيب الصف الصحيح
- **رأس الجدول صفٌّ واحد**: العنوان أولاً، وتحته `.th-tools` صفٌّ هادئ من أيقونتين
  مسطّحتين بلا خلفيات: `▼` الفلتر · `⇅` الترتيب.
  صف الفلاتر المستقل (`tr.th-filters`) أُزيل، فنزل ارتفاع الرأس من ~126px إلى ~70px
- **زر حذف العمود `×` أُزيل من الرأس** — كان يُضغط بالخطأ أثناء تعديل العناوين.
  الحذف صار من ⚙️ → «🗑️ حذف عمود…» (`pickDelCol`) التي تعرض قائمة الأعمدة للاختيار،
  و`delLastCol` حُذفت لأنها صارت بلا مستدعٍ
- **`colW(idx,ci)` وزن العمود حسب محتواه**: العدد 62 · الأيام 86 · الحالة/النوع 84
  · تاريخ 100 · نصّي 112 · عمود الحذف `DEL_COL_W`
- **`table-layout:fixed` وأعرض بالنسبة المئوية** — `renderDetTable` تجمع الأوزان
  وتحوّلها إلى `%` مجموعها 100، فيملأ الجدول عرض الحاوية **تماماً بلا تمرير أفقي**
  على الشاشات العريضة. `min-width:900px` على الجدول يبقي التمرير على الجوال والتابلت
  - **بالبكسل لا يعمل**: `table-layout:fixed` يوسّع الجدول لمجموع الأعرض إن تجاوزت
    الحاوية بدل أن يقلّصها — النسبة المئوية وحدها تضمن الاحتواء
  - `overflow-wrap:anywhere` على الخلايا والعناوين، وإلا فرض رمز طويل بلا مسافات
    (`SKR_SCHOLARSHIP_S_4764`) توسيع عموده إلى 180px
- `.th-filt` أيقونة ▼ صغيرة و`select` شفاف فوقها (`position:absolute;opacity:0`)
  ليفتح قائمة المتصفح؛ تُلوَّن ذهبياً عند التفعيل
- عناوين الأعمدة `<textarea>` مثل الخلايا فتظهر كاملة ولا تُقتطع
  — **لا تنسَ استدعاء `autoGrow` على `#detail-thead textarea.th-input` أيضاً**،
  فبدونه تبقى العناوين بسطر واحد مقتطعة رغم أنها textarea
- **رأس الجدول صفّان**: `tr.th-titles` للعناوين و`tr.th-filters` للفلاتر.
  وضعُهما في صف واحد كان يجعل الفلاتر بارتفاعات مختلفة حسب طول كل عنوان
- كل الخلايا والعناوين والفلاتر **موسَّطة**، وارتفاع الصف موحّد (`tbody tr{height:46px}`)

## بطاقات المخرجات (الرئيسية)
- تحت مربعَي «المستهدف الكلي» و«المتبقي» شريط بعرض البطاقة كاملاً:
  **المحقق حتى الآن (تراكمي)** = `cumAct` / المستهدف — في الواجهة وتقرير PDF معاً

## م1 و م2 — جداول المتابعة (منطق مشترك)
- `TRACKED = [0,1]` — م1 (الاستراتيجيات) و م2 (المؤشرات) يتبعان نفس النمط
  ونفس مواقع الأعمدة، فيعملان بنفس دوال الحساب والعرض
- `recalcTracked(idx)` تحسب عمودي **الحالة** و**الأيام** من التواريخ،
  و`recalcStrategies()` تشغّلها على الاثنين — تُستدعى في `loadFromCloud()` و`renderDetails()`
  — العمودان للقراءة فقط في الجدول
- `isTracked(i)` تمنع الحساب إن غُيّر ترتيب الأعمدة (الحساب يعتمد على المواقع)
- أعمدة م2: اسم الجهة · النوع · الحالة · العدد · تاريخ الإرسال · المخطط · الاستلام · الأيام
  - «النوع» قائمة منسدلة: `M2_TYPES` = مؤشر / مبادرة / ملف مؤشرات / ملف مبادرات
  - «العدد» لعدد البنود داخل الملف الواحد (مثلاً 70 مؤشراً)
  - `مكتمل` = يوجد تاريخ استلام · `معلق` = لا يوجد تاريخ مخطط صالح
  - `متأخر` = المخطط مضى ولم يُستلم · `قيد التنفيذ` = المخطط لم يحن بعد
  - الأيام = (الاستلام − المخطط) أو (اليوم − المخطط) إن لم يُستلم
  - `pDate()` تقبل `YYYY-MM-DD` و`DD/MM/YYYY`؛ أي نص آخر (لايوجد، لم يتم التحديد) = لا تاريخ
- `ensureTrackedCols(idx)` تستكمل الأعمدة الناقصة: تُدرج «تاريخ التسليم المخطط»
  قبل عمود الاستلام، وتلحق «الأيام» و«الحالة» — تعمل على البيانات المحفوظة
  سحابياً أيضاً (م2 كان ينقصه الثلاثة فاختفت بطاقاته)
- `dcol()` تطابق بلا «ال» التعريف (`استلام`/`مخطط`/`حالة`/`أيام`) لتعمل مع
  تسميات مثل «تاريخ استلامها من الشركة»
- `renderStrategyExtras()` تعرض ٦ بطاقات لكلا المخرجين: **الإجمالي** + الحالات
  الأربع + **سُلِّم بعد الموعد** (مكتمل والاستلام بعد المخطط، مع متوسط التأخير)
  - البطاقات **قابلة للنقر**: تفلتر الجدول على تلك الحالة، و«الإجمالي» يمسح الفلاتر
- **البطاقات تعكس المفلتر**: تُحسب من `detShown[idx]` (الصفوف المعروضة) لا من كل
  الصفوف، وبطاقة الإجمالي تعرض «المفلتر / الكلي» عند وجود فلتر
  - `detLateOnly[idx]` حالة فلتر «سُلِّم بعد الموعد»
- قائمتا «وصلت ولم تُرسل» و«قادمة متوقعة» لـ م1 وحده
- `loadFromCloud()` تُعيد رسم صفحة التفاصيل إن كانت مفتوحة وقت اكتمال التزامن،
  وإلا بقيت البطاقات فارغة حتى يبدّل المستخدم التبويب
- `stratLists` تُحفظ في `app_settings` تحت المفتاح `strategy_lists`
  (صلاحية الكتابة `edit:details:1`)
- ثوابت الأعمدة: `D_STAT=2`, `D_PLAN=5`, `D_RECV=6`, `D_DAYS=7`

## حالة صرف الشهر
- `paidBadge(m)` شارة داخل بطاقة «الصرف الفعلي» — زر لمن يملك `edit:plan`، ونص ثابت لغيره
- `togglePaidM(m)` تقلب الحالة وتحفظ في `plan_months` فوراً
- تقرير PDF يعرضها نصاً ثابتاً (لا مربع اختيار)

## المتغيرات الرئيسية في الكود
- `MP` — خطة الكميات الشهرية {شهر: [م1,م2,م3,م4]}
- `acts` — المنجز الفعلي (مصفوفة `projectMonths`)
- `INV` — فواتير الصرف المخطط بدون ضريبة
- `padPlan()` — توسّع MP/INV/INV_M/acts/paidMonths/actualPay لتغطي `projectMonths`
- `TGT` — المستهدفات [60,290,1230,22]
- `UP` — أسعار الوحدة [81504,2400,768,172320] (من ملف الإكسل المعتمد)
- `MN` — أسماء الأشهر بالعربي
- `detCols` / `detRows` — جداول التفاصيل الأربعة
- `curM` — الشهر الحالي المحدد
- `paidMonths` — حالة الصرف الفعلي لكل شهر
- `actualPay` — قيم الصرف اليدوية (null = تلقائي)

## الصفحات الخمس
1. **الرئيسية** — شريط تقدم المشروع + المخطط مقابل المنجز (4 بطاقات) + ملخص الصرف + جدول المتابعة الشهري
2. **التقرير الشهري** — إدخال المنجز الفعلي + ملخص كل الأشهر
3. **التفاصيل** — 4 جداول قابلة للتعديل (لكل مخرج) مع فلتر وسحب وترتيب ولصق من Excel
4. **خطة الصرف** — الفواتير الـ18 قابلة للتعديل + تحميل/رفع قالب Excel
5. **فريق المشروع** — modal

## ميزات مطبقة (لا تكسرها عند التعديل)
- شريط تقدم المشروع الملون (أخضر/أصفر/أحمر حسب الفجوة)
- كل خلايا جدول المتابعة قابلة للتعديل (class: `edit-cell`)
- زر استيراد خطة Excel (`importMPlan`)
- checkbox "تم الصرف" (`togglePaid` + `paidMonths`)
- زر تعديل مدة المشروع (`editProjectDuration`)
- أسماء الشهور في شريط التنقل
- التفاصيل: ترقيم + سحب صفوف (`detDragStart`/`detDrop`) + لصق جدول (`pasteTable`) + فلتر نوع الاستراتيجية
- ~~تصدير/استيراد JSON~~ — أُزيل زرّاهما من الشريط العلوي (بلا فائدة عملية)
- تقرير PDF يُولَّد في تبويب جديد (`generateReport`) بغلاف هوية أداء

## التجاوب وأحجام الخطوط
- `.main` بلا `max-width` — يملأ عرض الشاشة (كان محدوداً بـ 1440px)
- **كل المقاسات بـ `rem`**، فحجم خط الجذر هو المفتاح الوحيد لتكبير الواجهة كلها:
  `html{15px}` أساساً، ثم 16 · 17 · 18 · 19.5 · 21 · 22px عند
  480 · 1024 · 1280 · 1600 · 1920 · 2400px
  (كان ثابتاً 14px وهو سبب صِغَر الخط على اللابتوب)
- `@media(max-width:1100px)` عمودان · `@media(max-width:820px)` عمود واحد لكل الشبكات
- على الجوال: الشريط العلوي يلتف، شريط التنقل يمرّر أفقياً، و`body{overflow-x:clip}`
  يمنع التمرير الأفقي للصفحة (استُخدم `clip` لا `hidden` حتى لا يكسر `position:sticky`)
- جداول التفاصيل: `min-width:960px` داخل حاوية تمرّر أفقياً
  — `.detail-tbl-wrap` كان فيه `overflow:hidden` يلغي `overflow-x:auto` فمنع التمرير
- خلايا التفاصيل `<textarea rows=1>` مع `autoGrow()` بدل `<input>` — تلتف على أكثر من سطر

## هوية أداء البصرية
- الأخضر الداكن: `#0d3d22` / المتوسط: `#155c35`, `#1e7a48`, `#29a361`
- الذهبي: `#c9a020`
- شعار أداء مضمّن base64 في الملف

## أخطاء سابقة تم حلها (تجنب تكرارها)
1. مفاتيح كائن MP يجب أن تكون بصيغة JS (`1:`) وليس JSON (`"1":`)
2. لا تضع تعليقات عربية `//` داخل مصفوفات البيانات — سببت SyntaxError على iOS
3. عند استبدال دوال بـ regex تأكد من عدم ترك بقايا كود قديم بعد `}` — سببت "Unexpected token"
4. اختبر دائماً بـ `node --check` بعد أي تعديل على الـ JS

## سير العمل
1. عدّل `site/project-dashboard.html`
2. تحقق: `./check.sh` من جذر المستودع (يقرأ `site/project-dashboard.html`)
3. `git commit` ثم `git push` — الدمج في `main` ينشر تلقائياً على
   Netlify (الرابط المعتمد) و GitHub Pages (الاحتياطي)
